import React from 'react';
import './Header.css';

const Header = () => (
  <header className="header">
    <div className="header-title">11th & 12th Student Chatbot</div>
    <nav className="header-nav">
      {/* Add navigation items if needed */}
    </nav>
  </header>
);

export default Header;
